# PHP Mail Configuratie voor Strato
# Eenvoudige configuratie voor Strato shared hosting

# UTF-8 encoding
AddDefaultCharset UTF-8

# Zorg dat PHP bestanden correct worden uitgevoerd
<FilesMatch "\.php$">
    SetHandler application/x-httpd-php
</FilesMatch>

# Security headers (alleen als mod_headers beschikbaar is)
<IfModule mod_headers.c>
    Header set X-Content-Type-Options "nosniff"
    Header set X-Frame-Options "SAMEORIGIN"
</IfModule>

